;; DGO definition file for My first Level
;; We use the convention of having a longer DGO name for levels without precomputed visibility.

;; the actual file name still needs to be 8.3
("MFL.DGO"                                      ;; 3 Letter name for the final level file
  ("static-screen.o" "static-screen")           
  ("my-first-level.go" "my-first-level")        ;; the .go of our level which will be created by the compiler
  )